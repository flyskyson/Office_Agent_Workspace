# PDF 文本提取器 - 分步实施指南

> 面对复杂项目，如何从零开始一步步实现？本指南将帮助你循序渐进地掌握整个系统。

---

## 📋 学习路线图

```
第一阶段：基础准备 (第1-2天)
  ↓
第二阶段：核心功能实现 (第3-5天)
  ↓
第三阶段：OCR 集成 (第6-7天)
  ↓
第四阶段：批量处理 (第8-9天)
  ↓
第五阶段：优化和完善 (第10天)
```

---

## 🎯 第一阶段：基础准备 (1-2天)

### 目标
- 搭建开发环境
- 理解基本概念
- 实现最简单的 PDF 文本提取

### 步骤 1.1：环境搭建

```bash
# 1. 创建项目文件夹
mkdir pdf_processor
cd pdf_processor

# 2. 创建虚拟环境（推荐）
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate

# 3. 安装核心依赖
pip install pdfplumber
```

### 步骤 1.2：Hello World 版本

创建文件 `step1_basic.py`：

```python
"""
第一步：最简单的 PDF 文本提取
目标：从单个 PDF 文件中提取文本
"""

import pdfplumber

def extract_text_from_pdf(pdf_path):
    """从 PDF 中提取文本"""
    try:
        with pdfplumber.open(pdf_path) as pdf:
            # 遍历每一页
            for page_num, page in enumerate(pdf.pages, 1):
                text = page.extract_text()
                if text:
                    print(f"=== 第 {page_num} 页 ===")
                    print(text)
                    print()
    except Exception as e:
        print(f"错误: {e}")

if __name__ == "__main__":
    # 测试：修改为你的 PDF 文件路径
    pdf_file = "test.pdf"
    extract_text_from_pdf(pdf_file)
```

**练习任务：**
- [ ] 准备一个测试用的 PDF 文件
- [ ] 运行脚本，观察输出
- [ ] 尝试不同类型的 PDF（文字版、扫描版）

### 步骤 1.3：保存到文件

创建文件 `step2_save_to_file.py`：

```python
"""
第二步：将提取的文本保存到文件
"""

import pdfplumber
from datetime import datetime

def extract_and_save(pdf_path, output_path):
    """提取文本并保存到文件"""
    all_text = []

    with pdfplumber.open(pdf_path) as pdf:
        for page_num, page in enumerate(pdf.pages, 1):
            text = page.extract_text()
            if text:
                all_text.append(f"=== 第 {page_num} 页 ===\n{text}")

    # 合并所有文本
    full_text = "\n\n".join(all_text)

    # 保存到文件
    with open(output_path, 'w', encoding='utf-8') as f:
        f.write(f"# PDF 文本提取结果\n\n")
        f.write(f"提取时间: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}\n\n")
        f.write(full_text)

    print(f"✓ 已保存到: {output_path}")
    print(f"✓ 总页数: {len(all_text)}")
    print(f"✓ 总字符数: {len(full_text)}")

if __name__ == "__main__":
    pdf_file = "test.pdf"
    output_file = "output.txt"
    extract_and_save(pdf_file, output_file)
```

**练习任务：**
- [ ] 运行脚本，查看生成的 output.txt
- [ ] 尝试提取不同的 PDF 文件
- [ ] 思考：如果 PDF 是扫描件会怎样？

---

## 🎯 第二阶段：核心功能实现 (3-5天)

### 目标
- 实现文件夹扫描
- 批量处理多个 PDF
- 添加错误处理

### 步骤 2.1：扫描文件夹

创建文件 `step3_scan_folder.py`：

```python
"""
第三步：扫描文件夹中的所有 PDF 文件
"""

from pathlib import Path

def get_pdf_files(folder_path):
    """获取文件夹中的所有 PDF 文件"""
    folder = Path(folder_path)

    if not folder.exists():
        raise FileNotFoundError(f"文件夹不存在: {folder_path}")

    # 查找 .pdf 和 .PDF 文件
    pdf_files = list(folder.glob("*.pdf")) + list(folder.glob("*.PDF"))

    return sorted(pdf_files)

if __name__ == "__main__":
    # 测试：修改为你的 PDF 文件夹路径
    folder = "pdfs"  # 创建一个测试文件夹，放入几个 PDF

    try:
        pdfs = get_pdf_files(folder)
        print(f"找到 {len(pdfs)} 个 PDF 文件:")
        for pdf in pdfs:
            print(f"  - {pdf.name}")
    except Exception as e:
        print(f"错误: {e}")
```

**练习任务：**
- [ ] 创建测试文件夹 `pdfs`
- [ ] 放入几个 PDF 文件
- [ ] 运行脚本，观察输出

### 步骤 2.2：批量处理

创建文件 `step4_batch_process.py`：

```python
"""
第四步：批量处理文件夹中的所有 PDF
"""

from pathlib import Path
import pdfplumber
from datetime import datetime

def extract_text(pdf_path):
    """提取单个 PDF 的文本"""
    try:
        with pdfplumber.open(pdf_path) as pdf:
            all_text = []
            for page_num, page in enumerate(pdf.pages, 1):
                text = page.extract_text()
                if text and text.strip():
                    all_text.append(f"--- 第 {page_num} 页 ---\n{text.strip()}")
                else:
                    all_text.append(f"--- 第 {page_num} 页 ---\n[无文本内容]")

            return "\n\n".join(all_text), True, "文本提取"
    except Exception as e:
        return f"错误: {str(e)}", False, "提取失败"

def process_folder(folder_path, output_file):
    """处理文件夹中的所有 PDF"""
    folder = Path(folder_path)
    pdf_files = sorted(folder.glob("*.pdf")) + sorted(folder.glob("*.PDF"))

    results = []

    for index, pdf_path in enumerate(pdf_files, 1):
        print(f"[{index}/{len(pdf_files)}] {pdf_path.name}...", end=" ")

        content, success, method = extract_text(pdf_path)
        results.append((pdf_path.name, content, success, method))

        if success:
            print(f"[OK] ({len(content)} 字符)")
        else:
            print(f"[FAILED]")

    # 保存结果
    with open(output_file, 'w', encoding='utf-8') as f:
        f.write("# PDF 批量提取结果\n\n")
        f.write(f"提取时间: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}\n\n")
        f.write(f"总文件数: {len(results)}\n")
        f.write(f"成功: {sum(1 for _, _, success, _ in results if success)}\n\n")
        f.write("---\n\n")

        for filename, content, success, method in results:
            f.write(f"## {filename}\n\n")
            f.write(f"**方式**: {method}\n\n")
            f.write(content if success else f"[错误] {content}")
            f.write("\n\n---\n\n")

    print(f"\n✓ 结果已保存到: {output_file}")

if __name__ == "__main__":
    folder = "pdfs"
    output = "提取结果.md"
    process_folder(folder, output)
```

**练习任务：**
- [ ] 运行脚本，处理多个 PDF
- [ ] 查看生成的 `提取结果.md`
- [ ] 测试放入一个损坏的 PDF，观察错误处理

### 步骤 2.3：添加命令行参数

创建文件 `step5_command_line.py`：

```python
"""
第五步：添加命令行参数支持
"""

import argparse
from pathlib import Path
import pdfplumber

def parse_arguments():
    """解析命令行参数"""
    parser = argparse.ArgumentParser(description='PDF 文本批量提取工具')
    parser.add_argument('-i', '--input', required=True,
                       help='PDF 文件夹路径')
    parser.add_argument('-o', '--output', default='提取结果.md',
                       help='输出文件名')
    return parser.parse_args()

def main():
    args = parse_arguments()

    folder = Path(args.input)
    if not folder.exists():
        print(f"错误: 文件夹不存在 - {args.input}")
        return 1

    print(f"扫描文件夹: {folder}")
    # ... 处理逻辑 ...

if __name__ == "__main__":
    main()
```

**练习任务：**
- [ ] 运行：`python step5_command_line.py -i pdfs`
- [ ] 运行：`python step5_command_line.py -i pdfs -o my_result.md`
- [ ] 尝试不提供参数，观察帮助信息

---

## 🎯 第三阶段：OCR 集成 (6-7天)

### 目标
- 安装 OCR 工具
- 实现 OCR 文字识别
- 集成到主流程

### 步骤 3.1：安装 OCR 环境

```bash
# 1. 安装 Python 库
pip install pytesseract pdf2image pillow

# 2. Windows 系统：下载安装 Tesseract-OCR
#    下载地址: https://github.com/UB-Mannheim/tesseract/wiki
#    安装到: C:\Program Files\Tesseract-OCR

# 3. Windows 系统：下载安装 Poppler
#    下载地址: https://github.com/oschwartz10612/poppler-windows/releases
#    解压到: C:\poppler

# 4. 下载中文语言包
#    下载 chi_sim.traineddata
#    放到 Tesseract 的 tessdata 目录
```

### 步骤 3.2：测试 OCR

创建文件 `step6_ocr_test.py`：

```python
"""
第六步：测试 OCR 功能
"""

try:
    from pdf2image import convert_from_path
    import pytesseract
    OCR_AVAILABLE = True
except ImportError:
    OCR_AVAILABLE = False
    print("警告: OCR 库未安装")

def ocr_test(pdf_path):
    """测试 OCR 识别"""
    if not OCR_AVAILABLE:
        print("OCR 功能不可用")
        return

    # Windows 路径配置（根据实际安装位置修改）
    import platform
    if platform.system() == 'Windows':
        pytesseract.pytesseract.tesseract_cmd = r'C:\Program Files\Tesseract-OCR\tesseract.exe'
        poppler_path = r'C:\poppler\Library\bin'

    print(f"正在处理: {pdf_path}")

    # PDF 转图片
    images = convert_from_path(pdf_path, dpi=200, poppler_path=poppler_path)
    print(f"转换为 {len(images)} 张图片")

    # OCR 识别第一页
    text = pytesseract.image_to_string(images[0], lang='chi_sim+eng')
    print("\n识别结果:")
    print(text[:200])  # 只显示前 200 个字符

if __name__ == "__main__":
    # 使用一个扫描版 PDF 测试
    ocr_test("scanned.pdf")
```

**练习任务：**
- [ ] 准备一个扫描版 PDF
- [ ] 运行测试脚本
- [ ] 如果报错，检查路径配置
- [ ] 调整 DPI（150/200/300）比较效果

### 步骤 3.3：集成 OCR 到主流程

```python
"""
第七步：智能判断 - 文本提取 vs OCR
"""

def extract_with_fallback(pdf_path):
    """智能提取：文本提取失败则使用 OCR"""

    # 1. 先尝试文本提取
    try:
        with pdfplumber.open(pdf_path) as pdf:
            all_text = []
            for page in pdf.pages:
                text = page.extract_text()
                if text:
                    all_text.append(text)

            full_text = "\n".join(all_text)

            # 2. 如果文本太少，尝试 OCR
            if len(full_text.strip()) < 50:
                print("  文本过少，尝试 OCR...")
                return extract_with_ocr(pdf_path)
            else:
                return full_text, True, "文本提取"

    except Exception as e:
        print(f"  文本提取失败: {e}")
        return extract_with_ocr(pdf_path)
```

---

## 🎯 第四阶段：批量处理 (8-9天)

### 目标
- 完善批量处理逻辑
- 添加进度显示
- 实现统计功能

### 步骤 4.1：添加进度条

```bash
pip install tqdm
```

```python
from tqdm import tqdm

for pdf_path in tqdm(pdf_files, desc="处理 PDF"):
    # 处理逻辑
    pass
```

### 步骤 4.2：完整实现

将前面所有步骤整合成完整的 `pdf_extractor.py`（参考你现有的学习版代码）

---

## 🎯 第五阶段：优化和完善 (10天)

### 目标
- 代码优化
- 添加单元测试
- 编写文档

### 步骤 5.1：性能优化

```python
# 使用多进程加速
from multiprocessing import Pool

def process_single_pdf(pdf_path):
    # 处理单个 PDF
    pass

with Pool(processes=4) as pool:
    results = pool.map(process_single_pdf, pdf_files)
```

### 步骤 5.2：添加日志

```python
import logging

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(levelname)s - %(message)s',
    filename='pdf_extractor.log'
)

logging.info("开始处理...")
```

---

## 📚 学习检查清单

### 第一阶段检查点
- [ ] 能够提取单个 PDF 的文本
- [ ] 能够将结果保存到文件
- [ ] 理解 `with` 语句的作用
- [ ] 理解 `try-except` 异常处理

### 第二阶段检查点
- [ ] 能够扫描文件夹
- [ ] 能够批量处理多个 PDF
- [ ] 能够使用命令行参数
- [ ] 理解 `Path` 对象的使用

### 第三阶段检查点
- [ ] 成功安装 OCR 环境
- [ ] 能够识别扫描版 PDF
- [ ] 理解 OCR 的工作原理
- [ ] 能够智能选择提取方式

### 第四阶段检查点
- [ ] 实现完整的批量处理
- [ ] 添加进度显示
- [ ] 生成统计报告
- [ ] 支持 Markdown 和 JSON 输出

### 第五阶段检查点
- [ ] 代码经过优化
- [ ] 添加错误日志
- [ ] 编写使用文档
- [ ] 打包成可执行文件

---

## 🎓 学习建议

### 每日学习计划

**Day 1-2: 基础**
- 上午：学习 Python 基础语法（2小时）
- 下午：完成 step1-step2（3小时）
- 晚上：复习和总结（1小时）

**Day 3-5: 核心**
- 上午：学习文件操作和 Path 对象（2小时）
- 下午：完成 step3-step5（3小时）
- 晚上：调试和测试（1小时）

**Day 6-7: OCR**
- 上午：安装和配置 OCR 环境（2小时）
- 下午：完成 step6-step7（3小时）
- 晚上：测试不同类型 PDF（1小时）

**Day 8-9: 整合**
- 上午：整合所有功能（2小时）
- 下午：添加进度和统计（3小时）
- 晚上：完整测试（1小时）

**Day 10: 完善**
- 上午：优化代码（2小时）
- 下午：编写文档（3小时）
- 晚上：总结和反思（1小时）

### 调试技巧

1. **打印调试**
```python
print(f"调试: 变量值 = {variable}")
```

2. **分段测试**
```python
# 不要一次写完所有功能
# 先测试第一部分
# 确认无误后再写下一部分
```

3. **使用小样本**
```python
# 只处理 1-2 个文件测试
pdf_files = pdf_files[:2]
```

4. **错误日志**
```python
import traceback
try:
    # 代码
except Exception as e:
    traceback.print_exc()  # 打印详细错误信息
```

---

## 🚀 下一步

完成这个项目后，你可以：

1. **扩展功能**
   - 添加图片提取
   - 生成 Word 文档
   - 添加 PDF 加密支持

2. **性能优化**
   - 多进程处理
   - 异步 I/O
   - 缓存机制

3. **打包发布**
   - 使用 PyInstaller 打包
   - 发布到 PyPI
   - 制作安装程序

4. **学习更多**
   - Web 界面（Flask/Django）
   - 数据库存储
   - 机器学习文本分析

---

## 💡 遇到问题怎么办？

### 常见问题

**Q: OCR 识别率很低？**
A: 尝试调整 DPI（200→300），使用更好的扫描件，或使用更专业的 OCR 引擎

**Q: 中文显示乱码？**
A: 确保文件使用 UTF-8 编码：`open(..., encoding='utf-8')`

**Q: 处理速度太慢？**
A: 降低 DPI，使用多进程，或跳过 OCR

**Q: 某些 PDF 无法处理？**
A: 检查 PDF 是否加密、损坏，或使用 OCR 降级

### 获取帮助

1. 查看官方文档
2. Google 错误信息
3. GitHub Issues
4. Stack Overflow
5. 论坛和社区

---

祝你学习顺利！记住：**一步一步来，不要着急，每个阶段都要扎实掌握**。
